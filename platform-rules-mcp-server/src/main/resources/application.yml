server:
  port: 8081

spring:
  application:
    name: platform-rules-mcp-server
  ai:
    mcp:
      server:
        protocol: STREAMABLE
        name: platform-rules-mcp-server
        version: 1.0.0
        type: SYNC
        instructions: 提供示例平台的商品视频发布规则
        capabilities:
          resource: false
          prompt: false
          completion: false
          tool: true
        streamable-http:
          mcp-endpoint: /mcp

logging:
  level:
    com.example.platformrules: INFO
